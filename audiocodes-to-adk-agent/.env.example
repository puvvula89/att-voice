GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_PROJECT=your-project
GOOGLE_CLOUD_PROJECT_NUMBER=your-project-number
GOOGLE_CLOUD_LOCATION=us-central1
LIVE_MODEL=gemini-live-2.5-flash-native-audio
LIVE_VOICE=Charon
# CX Agent Studio (billing)
CES_APP=projects/your-project/locations/us/apps/your-app
CES_LOCATION=us
# Agent Engine (ADK agents)
# STAGING_BUCKET is optional: leave blank to auto-derive a globally-unique
# bucket ({project}-{project_number}-agent-engine), created on deploy if missing.
STAGING_BUCKET=
AE_ENGINE_ID=
# AudioCodes VoiceAI Connect (Phase 2 telephony). Shared Bearer token the bot
# provider sends on the /audiocodes-ws WebSocket; leave blank to disable auth
# (local/dev only — always set it for a shared/deployed relay).
AUDIOCODES_TOKEN=
